<%= simple_form_for message, url: message_reply_path(message), method: :post, wrapper: :vertical_form do |f| %>
  <%= f.error_notification %>
  <div class="form-inputs">
    test
    <%= f.input :body, label: false, placeholder: 'Send a reply', input_html: { maxlength: 2000 } %>
  </div>
  <div class="row">
    <%= f.button :submit, 'Send message', class: 'btn btn-success' %>
  </div>
<% end %>
