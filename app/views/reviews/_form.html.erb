<%= simple_form_for [@job, @review] do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :description %>
    <%#= f.association :job, collection: @job.contractor, label: 'Contractor' %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
