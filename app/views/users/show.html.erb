<%= image_tag @user.image(:original), class: 'img-rounded pull-left' if @user.image.exists? %>
<h3>Whatever</h3>
<p>Name: <%= @user.name if @user.name %></p>
<p>Email: <%= @user.email if @user.email %></p>
