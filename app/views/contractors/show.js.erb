$('#paginator-jobs-top').html('<%= escape_javascript(paginate @jobs,
																															 remote: true,
																															  theme: "twitter-bootstrap-3",
																													 param_name: "jobs",
																															 params: { tab: "jobs" }) %>');

$('#paginator-jobs-bottom').html('<%= escape_javascript(paginate @jobs,
																																 remote: true,
																															 		theme: "twitter-bootstrap-3",
																														 param_name: "jobs",
																															   params: { tab: "jobs" }) %>');

$('#jobs').html('<%= escape_javascript(render partial: "jobs/job_panel",
																					 collection: @jobs,
																									 as: :job) %>');

$('#paginator-examples-top').html('<%= escape_javascript(paginate @examples, remote: true,
																					                                     theme: "twitter-bootstrap-3",
																					                                param_name: "examples",
																					                                		params: { tab: "examples" }) %>');

$('#paginator-examples-bottom').html('<%= escape_javascript(paginate @examples, remote: true,
																						                                      theme: "twitter-bootstrap-3",
																						                                 param_name: "examples",
																						                                 		 params: { tab: "examples" }) %>');

$('#examples').html('<%= escape_javascript(render partial: "examples/example_panel", collection: @examples, as: :example) %>');

$('#paginator-comments-top').html('<%= escape_javascript(paginate @comments, remote: true,
																					                                     theme: "twitter-bootstrap-3",
																					                                param_name: "comments",
																					                                		params: { tab: "comments" }) %>');

$('#paginator-comments-bottom').html('<%= escape_javascript(paginate @comments, remote: true,
																						                                      theme: "twitter-bootstrap-3",
																						                                 param_name: "comments",
																						                                 		 params: { tab: "comments" }) %>');

$('#comments').html('<%= escape_javascript(render partial: "comments/comment_panel",
																			         collection: @comments,
																			                 as: :comment) %>');


$('#paginator-bids-top').html('<%= escape_javascript(paginate @bids, remote: true,
																																			theme: "twitter-bootstrap-3",
																																 param_name: "bids",
																																		 params: { tab: "bids" }) %>');

$('#paginator-bids-bottom').html('<%= escape_javascript(paginate @bids, remote: true,
																																				 theme: "twitter-bootstrap-3",
																																		param_name: "bids",
																																				params: { tab: "bids" }) %>');

$('#bids').html('<%= escape_javascript(render partial: "bids/bid_panel",
																					 collection: @bids,
																									 as: :bid) %>');

$('body').scrollTo('.tab-content', { duration: 300 });
